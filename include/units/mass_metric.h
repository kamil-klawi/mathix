/**
 * @file mass_metric.h
 * @brief Functions for converting between metric mass units
 *
 * @date 18-12-2025
 */

#ifndef MATHIX_MASS_METRIC_H
#define MATHIX_MASS_METRIC_H

#include "../float_t.h"

/*
 * @brief Converts mass from tons to kilograms
 *
 * @param Mass in tons
 * @return Mass in kilograms
 */
float64_t convert_t_to_kg(float64_t t);

/*
 * @brief Converts mass from tons to dekagrams
 *
 * @param Mass in tons
 * @return Mass in dekagrams
 */
float64_t convert_t_to_dag(float64_t t);

/*
 * @brief Converts mass from tons to grams
 *
 * @param Mass in tons
 * @return Mass in grams
 */
float64_t convert_t_to_g(float64_t t);

/*
 * @brief Converts mass from tons to milligrams
 *
 * @param Mass in tons
 * @return Mass in milligrams
 */
float64_t convert_t_to_mg(float64_t t);

/*
 * @brief Converts mass from tons to micrograms
 *
 * @param Mass in tons
 * @return Mass in micrograms
 */
float64_t convert_t_to_ug(float64_t t);

/*
 * @brief Converts mass from kilograms to tons
 *
 * @param Mass in kilograms
 * @return Mass in tons
 */
float64_t convert_kg_to_t(float64_t kg);

/*
 * @brief Converts mass from kilograms to dekagrams
 *
 * @param Mass in kilograms
 * @return Mass in dekagrams
 */
float64_t convert_kg_to_dag(float64_t kg);

/*
 * @brief Converts mass from kilograms to grams
 *
 * @param Mass in kilograms
 * @return Mass in grams
 */
float64_t convert_kg_to_g(float64_t kg);

/*
 * @brief Converts mass from kilograms to milligrams
 *
 * @param Mass in kilograms
 * @return Mass in milligrams
 */
float64_t convert_kg_to_mg(float64_t kg);

/*
 * @brief Converts mass from kilograms to micrograms
 *
 * @param Mass in kilograms
 * @return Mass in micrograms
 */
float64_t convert_kg_to_ug(float64_t kg);

/*
 * @brief Converts mass from dekagrams to tons
 *
 * @param Mass in dekagrams
 * @return Mass in tons
 */
float64_t convert_dag_to_t(float64_t dag);

/*
 * @brief Converts mass from dekagrams to kilograms
 *
 * @param Mass in dekagrams
 * @return Mass in kilograms
 */
float64_t convert_dag_to_kg(float64_t dag);

/*
 * @brief Converts mass from dekagrams to grams
 *
 * @param Mass in dekagrams
 * @return Mass in grams
 */
float64_t convert_dag_to_g(float64_t dag);

/*
 * @brief Converts mass from dekagrams to milligrams
 *
 * @param Mass in dekagrams
 * @return Mass in milligrams
 */
float64_t convert_dag_to_mg(float64_t dag);

/*
 * @brief Converts mass from dekagrams to micrograms
 *
 * @param Mass in dekagrams
 * @return Mass in micrograms
 */
float64_t convert_dag_to_ug(float64_t dag);

/*
 * @brief Converts mass from grams to tons
 *
 * @param Mass in grams
 * @return Mass in tons
 */
float64_t convert_g_to_t(float64_t g);

/*
 * @brief Converts mass from grams to kilograms
 *
 * @param Mass in grams
 * @return Mass in kilograms
 */
float64_t convert_g_to_kg(float64_t g);

/*
 * @brief Converts mass from grams to dekagrams
 *
 * @param Mass in grams
 * @return Mass in dekagrams
 */
float64_t convert_g_to_dag(float64_t g);

/*
 * @brief Converts mass from grams to milligrams
 *
 * @param Mass in grams
 * @return Mass in milligrams
 */
float64_t convert_g_to_mg(float64_t g);

/*
 * @brief Converts mass from grams to micrograms
 *
 * @param Mass in grams
 * @return Mass in micrograms
 */
float64_t convert_g_to_ug(float64_t g);

/*
 * @brief Converts mass from milligrams to tons
 *
 * @param Mass in milligrams
 * @return Mass in tons
 */
float64_t convert_mg_to_t(float64_t mg);

/*
 * @brief Converts mass from milligrams to kilograms
 *
 * @param Mass in milligrams
 * @return Mass in kilograms
 */
float64_t convert_mg_to_kg(float64_t mg);

/*
 * @brief Converts mass from milligrams to dekagrams
 *
 * @param Mass in milligrams
 * @return Mass in dekagrams
 */
float64_t convert_mg_to_dag(float64_t mg);

/*
 * @brief Converts mass from milligrams to grams
 *
 * @param Mass in milligrams
 * @return Mass in grams
 */
float64_t convert_mg_to_g(float64_t mg);

/*
 * @brief Converts mass from milligrams to micrograms
 *
 * @param Mass in milligrams
 * @return Mass in micrograms
 */
float64_t convert_mg_to_ug(float64_t mg);

/*
 * @brief Converts mass from micrograms to tons
 *
 * @param Mass in micrograms
 * @return Mass in tons
 */
float64_t convert_ug_to_t(float64_t ug);

/*
 * @brief Converts mass from micrograms to kilograms
 *
 * @param Mass in micrograms
 * @return Mass in kilograms
 */
float64_t convert_ug_to_kg(float64_t ug);

/*
 * @brief Converts mass from micrograms to dekagrams
 *
 * @param Mass in micrograms
 * @return Mass in dekagrams
 */
float64_t convert_ug_to_dag(float64_t ug);

/*
 * @brief Converts mass from micrograms to grams
 *
 * @param Mass in micrograms
 * @return Mass in grams
 */
float64_t convert_ug_to_g(float64_t ug);

/*
 * @brief Converts mass from micrograms to milligrams
 *
 * @param Mass in micrograms
 * @return Mass in milligrams
 */
float64_t convert_ug_to_mg(float64_t ug);

#endif //MATHIX_MASS_METRIC_H